# symlink to ~/.R/Makevars.win
# --------
# Makevars.win
# vim: filetype=make
# --------

MAKE=make -j`nproc`

# standards
CXX1XSTD=-std=c++0x
CXX11STD=-std=c++11
CXX14STD=-std=c++14
CXX17STD=-std=c++17

STD_FLAGS=-g -O3 -Wall -pedantic -mtune=native -pipe
CFLAGS=$(STD_FLAGS)
CXXFLAGS=$(STD_FLAGS)
CXX98FLAGS=$(STD_FLAGS)
CXX11FLAGS=$(STD_FLAGS)
CXX14FLAGS=$(STD_FLAGS)
CXX17FLAGS=$(STD_FLAGS)

FFLAGS=$(STD_FLAGS)
FCFLAGS=$(STD_FLAGS)
